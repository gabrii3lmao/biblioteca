<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= pageTitle %>
    </title>
    <link rel="stylesheet" href="/css/catalog.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@500;600;700&display=swap"
        rel="stylesheet">

</head>

<body>
    <h1>
        <%= pageTitle %>
    </h1>
    <h2><a href="/livros/novo">Adicionar novos livros</a></h2>
    <% if (livros.length> 0) { %>
        <h2>Livros Cadastrados:</h2>

        <ul>
            <% livros.forEach(function(livro) { %>
                <li>
                    <div class="book-header">
                        <strong>
                            <%= livro.nome %>
                        </strong>
                        <span class="book-year">
                            <%= livro.anoLancamento %>
                        </span>
                    </div>

                    <div class="book-meta">
                        <span>
                            <%= livro.autor %>
                        </span>
                        <span>R$ <%= livro.preco.toFixed(2) %></span>
                    </div>

                    <p class="book-description">
                        <%= livro.descricao %>
                    </p>

                    <div class="book-actions">
                        <a href="/livros/editar/<%= livro.id %>">Editar</a>

                        <form action="/livros/<%= livro.id %>?_method=DELETE" method="POST">
                            <button type="submit"
                                onclick="return confirm('Tem certeza que deseja excluir este livro?');">
                                Deletar
                            </button>
                        </form>
                    </div>
                </li>

                <% }) %>
        </ul>
        <% } else { %>
            <p>Nenhum livro encontrado no catálogo.</p>
            <% } %>
                <hr>
                <footer>
                    <hr>
                    <p style="text-align: center; font-size: 0.9em; color: #777;">
                        Construído por gabrii3lmao. Código-fonte disponível no GitHub:
                        <a href="https://github.com/gabrii3lmao/biblioteca" target="_blank">gabrii3lmao/biblioteca</a>
                    </p>
                </footer>
<script src="/js/tooltips.js"></script>
</body>

</html>